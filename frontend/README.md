# DefenderMate — Frontend

Next.js 15 app with shadcn/ui, TanStack Query, and Recharts.

## Getting Started

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

App available at `http://localhost:3000`
